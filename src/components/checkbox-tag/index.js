import NeoCheckboxTag from './src/checkbox-tag';

/* istanbul ignore next */
NeoCheckboxTag.install = function(Vue) {
  Vue.component(NeoCheckboxTag.name, NeoCheckboxTag);
};

export default NeoCheckboxTag;
